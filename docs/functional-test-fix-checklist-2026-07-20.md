# 功能测试修复清单（2026-07-20）

## 已完成

- [x] 默认关闭演示业务数据初始化；仅允许通过 `CAM_SEED_DEMO_DATA=1` 显式开启。
- [x] 删除缺失附件的伪造 SVG 证书逻辑；不存在的附件返回 404。
- [x] 删除资质 OCR 按文件名返回虚构证照信息的降级逻辑。
- [x] 上传文件限制为 PDF、常见图片或 XLSX，并同时校验扩展名和文件头；单次请求默认限制 20MB。
- [x] 增加 CSP、防点击劫持、MIME 嗅探、来源策略和浏览器权限策略响应头。
- [x] 登录连续失败 5 次后锁定 15 分钟；轮询 OCR 状态不再续期登录会话。
- [x] 无效月份、日期、金额、手机号、身份证格式及 JSON 参数返回 400，不再产生 500。
- [x] 凭证改为“作废留痕”，作废记录不计入看板和 Excel 财务台账，且不能再次编辑。
- [x] 增加关键操作审计表，并在超级管理员“系统设置 → 审计日志”中展示。
- [x] 项目存在凭证或合同时禁止删除，避免级联破坏历史台账。
- [x] 删除人员、合同、资质和未确认批量任务时同步清理物理附件。
- [x] Excel 导出转义公式型文本，防止表格公式注入。
- [x] SQLite 启用 WAL、10 秒锁等待和关键查询索引，降低并发写入锁冲突。
- [x] 人员档案与考勤页改为按入口加载，避免同时渲染两套大型页面内容。
- [x] OCR 先校验/解析文件再判断 API 配置，使损坏 PDF 得到真实错误提示。

## 上线前操作（尚未执行）

- [ ] 备份线上 SQLite 数据库和 `uploads/` 目录。
- [ ] 发布本修复分支并运行数据库自动迁移。
- [ ] 将 Nginx `ssl_protocols` 调整为仅 `TLSv1.2 TLSv1.3`，同步设置 20MB 上传上限。
- [ ] 确认生产环境未设置 `CAM_SEED_DEMO_DATA=1`。
- [ ] 由业务负责人核对并人工清理现有演示记录；程序不会自动删除，避免误伤真实数据。
- [ ] 发布后复测登录、项目、凭证作废、附件上传/下载、考勤、四类 Excel 导出和移动端页面。

## 数据说明

本次代码修复不会自动删除现有人员、项目、凭证、合同或附件记录。线上库中已存在的演示数据必须先由业务人员确认范围，再单独执行清理。
